# Aturan ProGuard/R8 khusus proyek ini bisa ditambahkan di sini.
# Saat ini minifyEnabled = false untuk build release, jadi file ini
# belum aktif dipakai — disiapkan untuk saat minifikasi diaktifkan nanti.
