// Top-level build file. Deklarasi plugin di sini, versi aktualnya dipasang
// per-modul di app/build.gradle.kts (lihat blok `plugins { }` di sana).
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
